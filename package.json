{
  "name": "linden",
  "version": "0.0.1",
  "description": "Code for talk about turtles & l-systems",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch": "watchify index.js -o static/bundle.js -dv",
    "build": "NODE_ENV=production browserify index.js | uglifyjs -m -c > static/bundle.js"
  },
  "author": "arikba",
  "license": "MIT",
  "dependencies": {}
}
